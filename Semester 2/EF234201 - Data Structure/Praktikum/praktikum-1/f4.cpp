#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    vector<int> in;
    vector<int> out;

    int n, k;
    cin >> n >> k;

    while (n != 0 && k != 0)
    {
        int p, q;
        
        cin >> n >> k;
    }

    return 0;
}
