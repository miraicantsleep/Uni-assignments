#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdbool.h>

int main()
{
    int t, n, k;
    scanf("%d", &t);
    while (t--)
    {
        scanf("%d %d", &n, &k);
    }
}